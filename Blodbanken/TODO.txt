Lag funksjonalitet mot database og knytt opp mot GUI 			
	+Book donortime
		GUI skal populeres med info fra database
			data fra database legges nå som json på sida,
			må kunne legge inn data i database for at denne skal kunne vises og brukes
		GUI skal kunne oppdatere i database
			Må testes
		Bookeren må testes for brukers egen booking og for booking for andre
	+timebooking 	
		GUI skal populeres med info fra database
			data fra database legges nå som json på sida,
			må kunne legge inn data i database for at denne skal kunne vises og brukes
		GUI skal kunne oppdatere i database
			Må testes
		Bookeren må testes for brukers egen booking og for booking for andre
	+parkeringsbooking - 
		(tog)Det må legges til en dropdownliste som viser hvilke timer man skal kunne booke parkering for.
		GUI må ha info om ledig parkering ved valg av dato, og deretter mulighet for valg av time
		Tabell kan bare velges fra, hvis man har valgt en time å booke parkering for
		Hvis man velger en time med en parkeringsbooking, skal denne parkeringsbookignen vises og kunne endres
		GUI skal kunne lagre parkering til database knyttet opp mot en booking
	+Lag den automatiske bookeren, og la den kunne konfigureres

*Oppdater bruker i AdminArea.aspx, da lukker alle kontroller seg etterpå, dette er ikke korrekt.
	Grunnen er at GetPostBackControlId ikke klarer å finne knapper
*Extension method GetPostBackControlId klarer ikke å finne knapper, har prøvd å fikse det.
*Av og pålogging fungerer ikke som tiltenkt
*Fjern approved fra egenerklæringen i database og klassedefinisjon.